{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window. `opener:allow-open-path` is scope-gated: with no allow list every path is refused and the call fails silently. Books are written beside their source PDF, which the user picks and which may be on any drive, so the scope cannot be narrowed to a fixed base directory.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "opener:default",
    {
      "identifier": "opener:allow-open-path",
      "allow": [{ "path": "**" }]
    },
    "opener:allow-reveal-item-in-dir",
    "dialog:default",
    "dialog:allow-open",
    "dialog:allow-message"
  ]
}
